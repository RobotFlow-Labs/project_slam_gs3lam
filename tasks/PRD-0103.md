# Task PRD-0103: Implement Dataset Registry

> PRD: PRD-01 | Depends: PRD-0102
> Files: 5 | Lines: ~300 | Time: ~90min

## What to Build
Port Replica, ScanNet, and TUM dataset loading logic into the ANIMA package with a shared registry interface.

## Files
### Create: `src/anima_slam_gs3lam/datasets/registry.py`
```python
def build_dataset(name: str, cfg: DatasetConfig):
    ...
```

### Create: `tests/test_datasets.py`
```python
def test_frame_batch_contract():
    assert batch.rgb.shape[0] == 3
```

## Verify
```bash
uv run pytest tests/test_datasets.py -v
```
