# Task PRD-0202: Wrap Semantic Splatting

> PRD: PRD-02 | Depends: PRD-0201
> Files: 2 | Lines: ~170 | Time: ~75min

## What to Build
Implement the rasterizer wrapper that returns RGB, depth, semantic features, and opacity, then decode semantics with a 1x1 conv.

## Files
### Create: `src/anima_slam_gs3lam/rendering/rasterizer.py`
```python
def render_field(field, pose, intrinsics, image_size):
    return RenderOutputs(...)
```

### Create: `src/anima_slam_gs3lam/semantic/decoder.py`
```python
class SemanticDecoder(nn.Module):
    ...
```

## Verify
```bash
uv run pytest tests/test_decoder.py -v
```
