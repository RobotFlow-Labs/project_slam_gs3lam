# Task PRD-0503: Containerize The Service

> PRD: PRD-05 | Depends: PRD-0502
> Files: 2 | Lines: ~120 | Time: ~45min

## What to Build
Add a CUDA-ready Dockerfile and compose manifest for local service deployment.

## Files
### Create: `docker/Dockerfile.cuda`
```dockerfile
FROM nvidia/cuda:11.7.1-cudnn8-runtime-ubuntu22.04
```

### Create: `docker/docker-compose.api.yml`
```yaml
services:
  gs3lam-api: {}
```

## Verify
```bash
docker compose -f docker/docker-compose.api.yml up --build
```
