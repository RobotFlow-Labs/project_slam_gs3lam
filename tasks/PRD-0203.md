# Task PRD-0203: Add DSR And Core Loss Blocks

> PRD: PRD-02 | Depends: PRD-0202
> Files: 2 | Lines: ~160 | Time: ~60min

## What to Build
Implement the DSR scale penalties and unit tests covering large/small Gaussian constraints.

## Files
### Create: `src/anima_slam_gs3lam/losses/regularization.py`
```python
def depth_adaptive_scale_regularization(scales: torch.Tensor) -> dict[str, torch.Tensor]:
    ...
```

### Create: `tests/test_regularization.py`
```python
def test_dsr_penalizes_outliers():
    ...
```

## Verify
```bash
uv run pytest tests/test_regularization.py -v
```
