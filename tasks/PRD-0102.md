# Task PRD-0102: Define Settings And Contracts

> PRD: PRD-01 | Depends: PRD-0101
> Files: 3 | Lines: ~180 | Time: ~60min

## What to Build
Add Pydantic settings for module, dataset, and optimization defaults, plus typed frame contracts.

## Files
### Create: `src/anima_slam_gs3lam/config.py`
```python
class GS3LAMConfig(BaseModel):
    dataset: DatasetConfig
    tracking: TrackingConfig
    mapping: MappingConfig
```

### Create: `src/anima_slam_gs3lam/types.py`
```python
class FrameBatch(NamedTuple):
    rgb: torch.Tensor
    depth: torch.Tensor
    semantic: torch.Tensor
    intrinsics: torch.Tensor
    pose: torch.Tensor
```

## Verify
```bash
uv run pytest tests/test_config.py -v
```
