# Task PRD-0401: Port Rendering Metrics

> PRD: PRD-04 | Depends: PRD-0303
> Files: 2 | Lines: ~180 | Time: ~60min

## What to Build
Implement PSNR, SSIM, LPIPS, and depth error aggregation with the same masking logic used by the reference evaluator.

## Files
### Create: `src/anima_slam_gs3lam/eval/rendering.py`
```python
def rendering_metrics(render_rgb, gt_rgb, render_depth, gt_depth):
    ...
```

### Create: `tests/test_metrics.py`
```python
def test_psnr_monotonicity():
    ...
```

## Verify
```bash
uv run pytest tests/test_metrics.py -v
```
