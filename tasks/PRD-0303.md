# Task PRD-0303: Implement RSKM And SLAM Loop

> PRD: PRD-03 | Depends: PRD-0302
> Files: 3 | Lines: ~260 | Time: ~90min

## What to Build
Add the random keyframe sampler and wire tracking plus mapping into a scene runner CLI.

## Files
### Create: `src/anima_slam_gs3lam/mapping/rskm.py`
```python
def sample_frame(keyframes, step_idx, t_opt):
    ...
```

### Create: `src/anima_slam_gs3lam/pipeline/slam_loop.py`
```python
class GS3LAMLoop:
    def step(self, frame): ...
```

## Verify
```bash
uv run pytest tests/test_rskm.py -v
uv run python scripts/run_replica.py --scene office0 --max-frames 10
```
