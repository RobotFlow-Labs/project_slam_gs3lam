# Docker — Dual Container Setup

## Every module MUST have:
```
docker/
├── Dockerfile.cuda      # NVIDIA CUDA training/inference
├── Dockerfile.mlx       # Apple Silicon MLX
└── docker-compose.yaml  # Both variants
```
